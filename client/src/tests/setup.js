/**
 * client/src/tests/setup.js
 *
 * Vitest global test setup for the frontend.
 * Imports @testing-library/jest-dom to extend Vitest's expect()
 * with DOM-specific matchers like toBeInTheDocument(), toHaveValue(), etc.
 */
import '@testing-library/jest-dom';
